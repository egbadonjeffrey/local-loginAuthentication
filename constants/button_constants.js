import * as color from "../constants/colors";

// export const button_colors = () => {
// }

const primary_color = color.COLORS.primary;
const white_color = color.COLORS.white;

export { primary_color, white_color };

export const buttonText = {
  default: "default text",
  sign_in: "sign in",
  sign_up: "sign up",
  sign_out: "sign out",
};
